# GUI Assets

Drop your custom app icon here as one of the following:

- `app.ico`
- `app.png`

`build_gui.py` will use one of these automatically for the packaged app icon
when you do not pass `--icon`.
The GUI will also pick it up automatically in development and packaged builds.
